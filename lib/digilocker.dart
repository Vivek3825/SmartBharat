import 'package:flutter/material.dart';

class DigiLockerPage extends StatelessWidget {
  const DigiLockerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DigiLocker')),
      body: const Center(child: Text('DigiLocker feature coming soon!')),
    );
  }
}

import 'package:flutter/material.dart';

class NewsUpdatePage extends StatelessWidget {
  const NewsUpdatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('News Update')),
      body: const Center(child: Text('News Update feature coming soon!')),
    );
  }
}
